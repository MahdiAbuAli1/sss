# Core API
fastapi~=0.115
uvicorn[standard]~=0.30

# LangChain stack
langchain~=0.3
langchain-community~=0.3
langchain-text-splitters~=0.3

# RAG dependencies
faiss-cpu~=1.8
sentence-transformers~=3.0
rank-bm25~=0.2

# Clients and utils
httpx~=0.27
requests~=2.32
python-dotenv~=1.0
pydantic~=2.8
numpy~=1.26

# Optional: Torch is required by sentence-transformers; install appropriate wheel for your platform
# If build fails, install torch first from https://pytorch.org/get-started/locally/
# torch
