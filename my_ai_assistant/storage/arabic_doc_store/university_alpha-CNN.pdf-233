mnist = tf.keras.datasets.mnist #رابتخالاو بيردتلا تانايب ليزنت (x_train, y_train), (x_test, y_test) = mnist.load_data() # رابتخالاو بيردتلا تانايب ددع ةعابط print (len(x_train)) print (len(x_test)) # x_train, x_test = x_train / 255.0, x_test / 255.0 # طشنلا يداحألا زيمرتلا y_train = [[i] for i in y_train] y_test = [[i] for i in y_test] enc = OneHotEncoder(sparse=True) enc.fit(y_train) y_train = enc.transform(y_train) y_test = enc.transform(y_test)